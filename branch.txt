1. Créer une branche dev

verifier les branches disponibles:
 git branch -a

Responsable:
git checkout main
git pull origin main
git checkout -b dev
git push -u origin dev

zay roa:
git fetch
git checkout dev
verifier les branches disponibles:
 git branch -averifier les branches disponibles:
 git branch -a
git checkout -b tsiky

git checkout dev
git pull origin dev
git checkout -b mira

travail de chacun:
git add .
git commit -m "Ajout fonctionnalité X"
git push origin tsiky

4. Fusion (merge) dans dev

Quand un ami a fini :
👉 Il fait une merge request / pull request vers dev
Ou en local :

git checkout dev
git pull origin dev
git merge feature-ami1
git push origin dev

verifier les branches disponibles:
 git branch -a